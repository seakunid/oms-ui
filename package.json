{
  "name": "seakun-oms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt --port 3001",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.0",
    "moment": "^2.27.0",
    "node-sass": "^4.14.1",
    "nuxt": "^2.14.0",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.2"
  },
  "devDependencies": {}
}
